apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: server-pvc
  namespace: anvilpowered-ci
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 128Gi
  storageClassName: anvilpowered-ci-storage
