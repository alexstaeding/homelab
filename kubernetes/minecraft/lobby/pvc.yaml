apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-lobby-pvc
  namespace: minecraft
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
  storageClassName: minecraft-storage
